{
  "name": "agent-live-widget-server",
  "version": "1.0.0",
  "private": true,
  "description": "Local state server for the Agent Live Widget — aggregates Claude Code hook events and broadcasts them over WebSocket.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
